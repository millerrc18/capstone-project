# AGENTS.md

## Tests
- bundle exec rubocop
- bundle exec brakeman
- bundle exec bundler-audit check --update
- bundle exec rspec
- bin/ui-screenshots

## Notes
- Rubocop uses `.rubocop_todo.yml` to track existing offenses; new offenses should be addressed directly.
